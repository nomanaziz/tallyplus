## Product Return system\n\nCurrently there is **no product return / refund** feature anywhere — adding a complete one.\n\n### 1. Database (migration)\n\nTwo new tables.\n\n**`sale_returns`** — one row per return event\n```\nid uuid pk\nshop_id uuid\nsale_id uuid (the original sale being returned against, nullable for walk-in returns)\ncustomer_id uuid (nullable)\nreturn_no text (auto: R-0001…)\nreason text (e.g. damaged, wrong item, customer changed mind, expired)\nreason_note text\ntotal numeric (sum of returned line totals)\nrefund_amount numeric (how much money actually given back)\nrefund_method payment_method ('cash', 'bkash', etc.)\nrefund_status text ('pending' | 'refunded' | 'adjusted_to_due')\nrestock boolean (default true — add qty back to stock)\nnote text\ncreated_by uuid\ncreated_at, updated_at, deleted_at\n```\n\n**`sale_return_items`** — line items of the return\n```\nid, return_id, product_id (nullable), name, qty, price, total, created_at\n```\n\nRLS: shop members manage their own; admins read.\nTrigger: `tg_set_updated_at`.\n\nWhen a return is created (handled in app code, not DB triggers, to keep it explicit):\n- Insert `sale_returns` + `sale_return_items`\n- For each item with `product_id` and `restock=true` → `products.stock += qty`\n- If `refund_status='refunded'` and `refund_method='cash'` → insert `cash_movements` row with `direction='out'`, `amount=refund_amount`, `ref_table='sale_returns'`, `ref_id=return.id` (this is the \